apiService:
  create: true
extraArgs:
  - --kubelet-insecure-tls
  - --kubelet-preferred-address-types=InternalIP

# Add resource specifications
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi